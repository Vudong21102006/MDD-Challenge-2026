
Thông tin file submission:

1. private_test_submission.csv: File csv với các cột dữ liệu: ```id,path```.
2. private_test_submission_example.csv: File submission ví dụ với các cột dữ liệu: ```id,path,predict```.

Lưu ý:
- File submission cần có đủ 3 cột ```id,path,predict``` và số hàng giống như file ```private_test_submission.csv```
- Đặt tên file là ```results.csv``` rồi nén zip và nộp trên AIHub